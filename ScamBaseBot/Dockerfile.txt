FROM python:3.10-slim

WORKDIR /app

COPY . .

# зависки 
RUN pip install --no-cache-dir --upgrade pip && \
    pip install --no-cache-dir python-telegram-bot==20.7 telethon==1.34.0

# Создаем папки
RUN mkdir -p /app/data /app/bot_images && \
    chmod 777 /app/data /app/bot_images

# Запус
CMD ["python", "ScamBaseBot/bot.py"]

